# a simple typeless function with typeless parameters declared to a typeless variable

# Output:
# 8
#

# Program:



begin
    f(a, b, c) is
        d = a + b + c;
        return d
    end
    x = call f(1, 3, 4);
    println x
end