# a function with typeless parameters inferred by arithmetic and exiting

# Output:
# 
#

# Program:

begin
  int f(a, b, c) is
    d = a + b;
    exit(c);
	return (b)
  end
  int x = call f(1, 2, 3)
end