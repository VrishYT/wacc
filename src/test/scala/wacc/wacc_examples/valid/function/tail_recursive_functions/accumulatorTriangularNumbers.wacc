begin
    int triangular(int acc, int x) is
        if (x <= 0)
        then return acc
        else
            int y = call triangular(acc + x, x - 1) ; 
            return y  
        fi
    end

    int x = call triangular(1, 5) ;
    exit x

end