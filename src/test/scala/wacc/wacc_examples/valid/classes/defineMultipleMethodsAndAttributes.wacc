# class contains multiple methods and attributes

# Output:

# Program:

begin
    class MyClass {
        int i,
        int j 
        
        int f(int x) is 
            return x + 1
        end
        int g(int x) is 
            return x + 2
        end
    }
    skip
end